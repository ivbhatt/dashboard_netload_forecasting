Steps to create run(linux) :

=> Create venv and activate it
python3 -m venv venv
source venv bin/activate

=> Install dependencies
pip install -r requirements.txt

=> Run the app-server
python3 app.py

